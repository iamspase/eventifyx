import { render } from "@testing-library/react"
import PricingCard from "./PricingCard"

describe("PricingCard", () => {
  it("Renders successfully", () => {
    render(<PricingCard name="" price="" features={[]} />)
  })
})